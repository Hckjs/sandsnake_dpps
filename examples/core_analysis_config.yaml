run_tag: "full_array"
user_config_dir: null
user_env_dir: null

targets:
  mc_simtel: false
  mc_dl1: true
  mc_dl1_merged: false
  mc_dl2: false
  rf_energy_regressor: false
  rf_particle_classifier: false
  rf_geometry_reconstructor: false
  rf_performance_plots: false
  rf_performance_plots: false
  rf_performance_plots: true
  cuts: true
  irfs: true
  irfs_plots: true

inputs:
  mc_simtel: null
  mc_dl1: /home/jonas/repos/sandsnake_dpps/data/CTAO/mc/dl1/
  mc_dl1_merged: null
  mc_dl2: null # Only if all three properties are reconstructed!
  models: null
  cuts: null
  irfs: null        # Both IRFs, cuts, and
  benchmarks: null  # benchmarks can be plotted using the "irfs_plots" target

# Gamma MC sample for both cut optimization and IRF computation.
# "gamma" selects point-like MC; "gamma_diffuse" selects diffuse MC.
irfs:
  gamma_particle: gamma

# [zen, az]
pointings:
  - [20, 180]

obstimes:
  # hours
  - 50
  - 5
  - 0.5

# RF training split
train_size: 0.5
cuts_size: 0.5

plugins: null #["fermi"]
